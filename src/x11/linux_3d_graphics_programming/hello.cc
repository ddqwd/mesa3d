#include <X11/Xlib.h>
#include <X11/Intrinsic.h>
#include <stdio.h>



int main(int argc, char *argv[])
{
	Display *dpy;
	// establish x server
	dpy = XOpenDisplay(NULL);

	// create and map an X window for output;
	Visual *vis;
	return 0;
}

